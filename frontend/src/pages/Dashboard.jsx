import { useAuth } from "@/hooks/useAuth"

function Dashboard() {
    const { user } = useAuth()

    return (
        <section className="space-y-6">
            
        </section>
    )
}

export default Dashboard
